<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Защита от мошенников - Справочник</title>
    <meta property="og:title" content="🛡️ Проверь себя: не дай мошенникам шанс!">
    <meta property="og:description" content="Интерактивный гид по безопасности: чат-бот, разбор схем обмана и тест.">
    <meta property="og:type" content="website">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        :root { --dark-blue: #1a3a4a; --light-blue: #2c5aa0; --danger: #e74c3c; --warning: #f39c12; --success: #27ae60; --light-bg: #f8f9fa; --white: #ffffff; --gray: #7f8c8d; --text: #2c3e50; }
        body { font-family: system-ui, -apple-system, sans-serif; background-color: var(--light-bg); color: var(--text); line-height: 1.6; }
        body.rtl { direction: rtl; text-align: right; }
        .header { background: linear-gradient(135deg, var(--dark-blue) 0%, var(--light-blue) 100%); color: var(--white); padding: 40px 20px; text-align: center; position: relative; }
        .language-selector { position: absolute; top: 10px; right: 10px; display: flex; gap: 10px; }
        .lang-btn { padding: 8px 12px; border: 2px solid var(--white); background: rgba(255,255,255,0.2); color: var(--white); border-radius: 4px; cursor: pointer; }
        .lang-btn.active { background: var(--white); color: var(--light-blue); }
        .nav-tabs { display: flex; overflow-x: auto; background: var(--white); border-bottom: 2px solid var(--light-blue); position: sticky; top: 0; z-index: 100; }
        .nav-tab { padding: 15px 25px; border: none; background: none; cursor: pointer; white-space: nowrap; border-bottom: 3px solid transparent; color: var(--gray); }
        .nav-tab.active { color: var(--light-blue); border-bottom-color: var(--light-blue); }
        .container { max-width: 1200px; margin: 0 auto; padding: 20px; }
        .section { display: none; }
        .section.active { display: block; }
        .card { background: var(--white); border-radius: 8px; padding: 20px; margin-bottom: 20px; border-left: 5px solid var(--warning); box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
        .card-content { display: none; margin-top: 10px; }
        .card-content.open { display: block; }
        .hotline { background: var(--danger); color: white; padding: 20px; border-radius: 8px; text-align: center; }
        .hotline-item { display: flex; justify-content: space-between; align-items: center; margin: 10px 0; background: rgba(0,0,0,0.1); padding: 10px; border-radius: 5px; }
        .chatbot-icon { position: fixed; bottom: 20px; right: 20px; width: 60px; height: 60px; background: var(--light-blue); border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 30px; z-index: 999; }
        .chatbot-window { position: fixed; bottom: 90px; right: 20px; width: 320px; height: 450px; background: var(--white); border-radius: 12px; box-shadow: 0 8px 24px rgba(0,0,0,0.2); display: none; flex-direction: column; z-index: 999; }
        .chatbot-window.open { display: flex; }
        .chatbot-messages { flex: 1; overflow-y: auto; padding: 10px; display: flex; flex-direction: column; gap: 10px; }
        .message { padding: 8px 12px; border-radius: 8px; max-width: 85%; }
        .message.bot { background: var(--light-bg); align-self: flex-start; }
        .message.user { background: var(--light-blue); color: white; align-self: flex-end; }
        .qr-modal { display: none; position: fixed; top: 0; left: 0; right: 0; bottom: 0; background: rgba(0,0,0,0.6); z-index: 1000; align-items: center; justify-content: center; }
        .qr-modal.open { display: flex; }
        .qr-content { background: white; padding: 20px; border-radius: 10px; text-align: center; }
    </style>
</head>
<body>
    <div class="language-selector">
        <button class="lang-btn active" onclick="setLanguage('ru')">РУС</button>
        <button class="lang-btn" onclick="setLanguage('he')">עברית</button>
    </div>
    <div class="header">
        <h1>🛡️ Защита от мошенников</h1>
    </div>
    <div class="nav-tabs">
        <button class="nav-tab active" onclick="switchTab('types')">📱 Типы</button>
        <button class="nav-tab" onclick="switchTab('signs')">🔍 Признаки</button>
        <button class="nav-tab" onclick="switchTab('help')">🆘 Помощь</button>
        <button class="nav-tab" onclick="switchTab('quiz')">❓ Тест</button>
    </div>
    <div class="container">
        <div id="types" class="section active">
            <div class="card" onclick="toggleCard(this)">
                <h3>📞 Телефонные звонки</h3>
                <p>Представляются банком или полицией.</p>
                <div class="card-content">Никогда не называйте коды из SMS и пароли!</div>
            </div>
        </div>
        <div id="help" class="section">
            <div class="hotline">
                <div class="hotline-item">
                    <span>🇷🇺 МВД: 8-800-700-40-30</span>
                    <button onclick="generateQR('tel:88007004030', 'МВД РФ')">QR</button>
                </div>
                <div class="hotline-item">
                    <span>🇮🇱 Полиция: 100</span>
                    <button onclick="generateQR('tel:100', 'Полиция Израиля')">QR</button>
                </div>
            </div>
        </div>
        <div id="quiz" class="section">
            <h3>Проверьте себя</h3>
            <p>Что делать при подозрительном звонке?</p>
            <button onclick="alert('Верно!')" style="width:100%; margin:10px 0; padding:10px;">Положить трубку и перезвонить самому</button>
        </div>
    </div>
    <div class="chatbot-icon" onclick="toggleChatbot()">💬</div>
    <div class="chatbot-window" id="chatbot">
        <div style="background:var(--light-blue); color:white; padding:10px;">🤖 Помощник</div>
        <div class="chatbot-messages" id="messages">
            <div class="message bot">Привет! Опишите ситуацию.</div>
        </div>
        <div style="display:flex; padding:10px; border-top:1px solid #ddd;">
            <input type="text" id="userInput" style="flex:1; padding:5px;">
            <button onclick="sendMessage()">></button>
        </div>
    </div>
    <div class="qr-modal" id="qrModal"><div class="qr-content"><div id="qrcode"></div><button onclick="closeQR()">Закрыть</button></div></div>
    <script>
        const responses = { 'пароль': 'Никому не говорите пароли!', 'default': 'Это похоже на мошенничество. Положите трубку.' };
        function toggleChatbot() { document.getElementById('chatbot').classList.toggle('open'); }
        function sendMessage() {
            const input = document.getElementById('userInput');
            const box = document.getElementById('messages');
            box.innerHTML += `<div class="message user">${input.value}</div>`;
            setTimeout(() => { box.innerHTML += `<div class="message bot">${responses.default}</div>`; }, 500);
            input.value = '';
        }
        function switchTab(id) {
            document.querySelectorAll('.section').forEach(s => s.classList.remove('active'));
            document.querySelectorAll('.nav-tab').forEach(t => t.classList.remove('active'));
            document.getElementById(id).classList.add('active');
            event.target.classList.add('active');
        }
        function toggleCard(card) { card.querySelector('.card-content').classList.toggle('open'); }
        function generateQR(txt, title) { 
            document.getElementById('qrcode').innerHTML = '';
            new QRCode(document.getElementById('qrcode'), { text: txt, width: 150, height: 150 });
            document.getElementById('qrModal').classList.add('open');
        }
        function closeQR() { document.getElementById('qrModal').classList.remove('open'); }
        function setLanguage(l) { document.body.className = (l === 'he' ? 'rtl' : ''); }
    </script>
</body>
</html>

